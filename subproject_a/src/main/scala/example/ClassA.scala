package example

/**
 * Some class in project A
 */
class ClassA {
  /**
   * returns the input parameter as the return value, unchanged
   * @param i Any integer
   * @return whatever was passed in as i
   */
  def echo(i: Int): Int = i
}
